version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.13"

python:
  install:
    - method: pip
      path: utils
      extra_requirements:
        - docs

mkdocs:
  configuration: utils/mkdocs.yml